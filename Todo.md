Question à poser : va-t'on utiliser le node package bootstrap ou bien bootstrap-sass ?

Il faut installer :

sass-loader (doc à lire + rajouter dans le package.json

-> node-sass + webpack en peerDependencies dans le package.json
